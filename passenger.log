import os
import sys

# Full path to your project root (where manage.py is located)
project_home = '/home/sarajac1/kalidjango/sales_mgmt_sys'

# Add project directory to sys.path if not already there
if project_home not in sys.path:
    sys.path.append(project_home)

# Set the Django settings module (points to inner sales_mgmt_sys/settings.py)
os.environ['DJANGO_SETTINGS_MODULE'] = 'sales_mgmt_sys.settings'

# Get WSGI application
from django.core.wsgi import get_wsgi_application
application = get_wsgi_application()

